# Note

To manually install the library, copy the files `max30102/circular_buffer.py`,  `max30102/max30102.py` and paste them here.

Then, load the content of `example` directory into the board.

The library included here is from n-elia available at:
https://github.com/n-elia/MAX30102-MicroPython-driver/
